version: '3'

services:
  app:
    build:
      context: .
      args:
        PHP_VERSION: ${PHP_VERSION}
    container_name: app
    environment:
      - BITRATE=${BITRATE}
    volumes:
        - './:/var/www/html/'
    ports:
        - "${APP_PORT}:80"
